<h1> Albuns Salvos </h1>

<table>
  <thead>
  <tr>
    <th> Nome </th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @musicians.each do |musician| %>
    <tr>
      <td><%= musician.name %></td>
      <td><%= link_to 'Editar', edit_musician_path(musician) %></td>
      <td><%= link_to 'Deletar', "/musicians/:#{musician.id}", method: :delete, data: {confirm: 'Can I do pop-ups?'} %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= link_to 'Novo músico', new_musician_path %>
<%= link_to 'Voltar', root_path %>
